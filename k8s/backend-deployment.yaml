apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: us-central1-docker.pkg.dev/amiable-archive-473111-d4/devops-repo/backend:v1
        ports:
        - containerPort: 5000
        env:
        - name: MYSQL_HOST
          value: "127.0.0.1"
        - name: MYSQL_USER
          value: "dbuser"
        - name: MYSQL_PASSWORD
          value: "dbpassword"
        - name: MYSQL_DB
          value: "testdb"
        - name: MYSQL_PORT
          value: "3306"
